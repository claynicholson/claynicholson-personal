# Project conventions

## Branching

Work directly on `main` by default. Do **not** create worktrees or feature branches unless I explicitly ask for one (e.g. "use a worktree", "make a branch for this", "open a PR"). For routine edits, commit straight to `main`.

If a task arrives in a worktree without me asking, exit it and operate on `main` instead.
